<!DOCTYPE HTML>
<html>
    <head>
        <title>Räume</title>
    </head>
    <body>
        <table>
            <% for (let i=0; i < rooms.length; i++) { %>
                <tr>
                    <td> <%= rooms[i].roomname %></td>
                    <td> <%= rooms[i].user %></td>
                    <td> <%= rooms[i].status %></td>
                    <td> 
                        <form action= "<%= "/delete/" + rooms[i]._id%>" method = "post"> 
                            <button type = "submit">Remove
                            </button>
                        </form>
                    </td>
                </tr>
            <% } %>
        </table>
        <a href="/">Startseite</a>
    </body>
</html>